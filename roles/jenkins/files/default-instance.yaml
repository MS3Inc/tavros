apiVersion: jenkins.io/v1alpha2
kind: Jenkins
metadata:
  name: tavros
  namespace: jenkins
spec:
  master:
    plugins:
      - name: keycloak
        version: 2.3.0
      # should be conditional of gitea enabled
      - name: gitea
        version: 1.2.1
      - name: build-user-vars
        version: 1.7
  configurationAsCode:
    configurations:
    - name: jenkins-configuration
