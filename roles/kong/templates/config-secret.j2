kind: Secret
apiVersion: v1
metadata:
  name: {{ item.name }}-config-secret
  namespace: kong
type: Opaque
data:
  pg_database: {{ item.name | b64encode }}
  pg_user: {{ item.name | b64encode }}
  pg_password: {{ kongs_db_info[ item.name ].pg_pass | b64encode }}
